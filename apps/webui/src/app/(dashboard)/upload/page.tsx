import type { Metadata } from 'next'
import Typography from '@mui/material/Typography'

export const metadata: Metadata = { title: 'Upload' }

export default function UploadPage() {
  return <Typography variant="h4">Upload</Typography>
}
